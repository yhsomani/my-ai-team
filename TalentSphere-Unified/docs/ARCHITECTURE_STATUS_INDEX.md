# TalentSphere Architecture Status Index

> Documentation status: Current architecture status and documentation precedence index. Keep synchronized with `../../PLAN.md`.

Date reviewed: 2026-06-29

This file is the current planning entry point for architecture status. It reconciles older architecture documents against the current repository state so product, UX, QA, and engineering work from the same assumptions.

## 1. Document Precedence

Use these documents in this order:

| Rank | Document | Use for |
|---:|---|---|
| 1 | `../../PLAN.md` | Rebuild execution SSOT, progress ledger, validation checklist, technical debt, and target architecture |
| 2 | `docs/ARCHITECTURE_STATUS_INDEX.md` | Current architecture status, document precedence, and open architecture decisions |
| 3 | `docs/COMPREHENSIVE_PRODUCT_UX_TECHNICAL_ANALYSIS_2026-06-26.md` | Product, UX, workflow, automation, technical roadmap, and implementation backlog |
| 4 | `docs/FEATURES_AND_DASHBOARDS.md` | Current feature inventory, routes, workflows, UI contents, data inputs, outputs, and role access |
| 5 | `docs/API_CONTRACT_MISMATCH_REPORT.md` | Generated frontend/backend/gateway/security route inventory |
| 6 | `docs/API_OPENAPI_CONTRACT.json` | Generated source-derived OpenAPI 3.1 route, parameter, request-body, response-body, and component-schema contract |
| 7 | `docs/runbooks/INCIDENT_RUNBOOKS.md` | Current source-backed incident runbooks for source validation, CI, scheduler, extension, API, auth, security, data, and admin degradation failures |
| 8 | `infra/observability/alerts/critical-alerts.json` and `infra/observability/dashboards/critical-flows-dashboard.json` | Source-level critical alert and dashboard coverage catalogs |
| 9 | `docs/PRODUCT_UX_AUTOMATION_AUDIT.md` | Running implementation history and UX automation audit notes |
| 10 | `docs/UX_AUDIT_CHECKLIST.md` | Current route, dashboard, and major-screen UX audit checklist for validated UI restructuring |
| 11 | `docs/DESIGN_SYSTEM.md` | Current design-system implementation guide for tokens, shared components, layout, navigation, and interaction patterns |
| 12 | `docs/MODULE_MANIFEST.md` | Source ownership, infrastructure, generated/dev artifact, and documentation lifecycle validation |
| 13 | `docs/adr/ADR-001-primary-identity-provider.md` | Accepted primary identity-provider decision and token-contract migration plan |
| 14 | `docs/adr/ADR-002-backend-topology.md` | Accepted backend topology decision: modular monolith first with extractable service boundaries |
| 15 | `docs/adr/ADR-003-schema-authority.md` | Accepted schema authority decision: migration-first Supabase/Postgres with generated TypeScript types and backend validation |
| 16 | `docs/adr/ADR-004-messaging-boundary.md` | Accepted messaging boundary decision: one messaging domain boundary with chat-service orphaned until retirement or adapter merge |
| 17 | `docs/adr/ADR-005-payment-mode.md` | Accepted payment mode decision: explicit demo billing mode until provider-backed checkout and webhook-owned state are verified |

Older architecture docs remain useful as historical context, but they should not be treated as current completion evidence unless this index confirms the same claim.

## 2. Current Architecture Snapshot

| Area | Current evidence | Current status |
|---|---|---|
| Frontend runtime | `apps/frontend` is the active React/Vite app; `apps/frontend/src/src` is not present | Single active frontend source tree |
| Frontend package manager | Root `package.json` has npm workspace scripts and root `package-lock.json`; no root `pnpm-lock.yaml` | Use npm commands unless a task explicitly targets another package manager |
| Frontend UX/design system | `docs/UX_AUDIT_CHECKLIST.md`, `docs/DESIGN_SYSTEM.md`, `scripts/validate-ui-design-system.mjs`, `apps/frontend/src/navigation/featureOwnership.ts`, `apps/frontend/tests/visual-layout.spec.ts`, `apps/frontend/tests/accessibility-semantics.spec.ts`, `apps/frontend/tests/auth-entry-workflow.spec.ts`, `apps/frontend/tests/not-found-recovery.spec.ts`, `apps/frontend/src/components/error/ErrorBoundary.tsx`, `apps/frontend/src/components/error/ErrorBoundary.test.tsx`, `apps/frontend/tests/keyboard-navigation.spec.ts`, `apps/frontend/tests/command-search-workflow.spec.ts`, `apps/frontend/tests/notification-workflow.spec.ts`, `apps/frontend/tests/dashboard-workflow.spec.ts`, `apps/frontend/tests/job-application.spec.ts`, `apps/frontend/src/pages/jobs/JobsPage.test.tsx`, `apps/frontend/tests/post-job-workflow.spec.ts`, `apps/frontend/src/pages/jobs/PostJobPage.test.tsx`, `apps/frontend/tests/profile-workflow.spec.ts`, `apps/frontend/src/lib/profileAvatarCrop.ts`, `apps/frontend/tests/resume-workflow.spec.ts`, `apps/frontend/tests/settings-workflow.spec.ts`, `apps/frontend/tests/billing-workflow.spec.ts`, `apps/frontend/tests/ai-assistant-workflow.spec.ts`, `apps/frontend/tests/career-path-workflow.spec.ts`, `apps/frontend/src/pages/ai/AICareerPath.test.tsx`, `apps/frontend/tests/admin-operations.spec.ts`, `apps/frontend/tests/lms-workflow.spec.ts`, `apps/frontend/tests/challenges-workflow.spec.ts`, `apps/frontend/src/pages/challenges/ChallengesPage.test.tsx`, `apps/frontend/tests/candidate-review.spec.ts`, `apps/frontend/tests/messaging-workflow.spec.ts`, `apps/frontend/tests/networking-workflow.spec.ts`, `apps/frontend/src/pages/networking/NetworkingPage.test.tsx`, `apps/frontend/src/index.css`, shared UI primitives, `LandingPage.tsx`, and `routeRegistry.ts` are present | Redesign execution is governed by a current UX audit checklist, design-system guide, source-level UI validator, feature ownership IA contract, major-route desktop/mobile layout audit, major-route accessibility semantics audit, an Auth entry workflow contract covering configured email login, inactive provider-control removal, accessible login errors, and registration role-intent selection, a Not Found recovery workflow contract covering public wildcard-route auth links, authenticated dashboard recovery, role-filtered app destinations, and route-audit coverage, a global ErrorBoundary unit contract covering safe fatal recovery copy, service-unavailable recovery, custom fallback passthrough, reload recovery, and no raw exception-message exposure, focused keyboard navigation guardrail covering command search, notifications including account read/load-more actions, mobile bottom and slide-over navigation, collapsed desktop sidebar naming, tabs, modals, and auth forms, a CommandSearch workflow contract covering label-ranked route discovery, role-filtered utility destinations, no-result states, and recruiter command-route navigation, a shell notification workflow contract covering account-sync labels, fallback source labels/retry, due-aware unread counts, scheduled reminder visibility, mark-all payloads, and read-failure rollback, a Dashboard workflow contract covering talent summary metrics, partial-data status and retry intent, recruiter summary metrics, role-specific primary actions, stat-card handoffs, quick-action handoffs, and workflow analytics, a Jobs workflow contract covering application review/submit/details behavior, saved-search create/apply/review-cancel/delete payloads, and hidden Explore hide/restore payloads, a Jobs Explore catalog load failure unit contract covering safe failed-load copy, raw error-message exclusion, and retry through the existing catalog refetch workflow, a Jobs Applied-tab recovery unit contract covering safe application history failed-load copy, raw provider-error exclusion, and retry through the existing application history load workflow, a Jobs My Posts recovery unit contract covering safe recruiter postings failed-load copy, raw provider-error exclusion, and retry through the existing recruiter job load workflow, a Jobs action-failure unit contract covering safe application-submit and recruiter publish failure copy, raw provider-error exclusion, and retry through existing Review Application and publish checklist workflows, a Jobs preference recovery unit contract covering safe saved-search and hidden Explore local-fallback copy, raw provider-error exclusion, and retained local controls, a Jobs browser-storage recovery unit contract covering safe saved-search and hidden Explore storage copy, raw quota/provider exclusion, and retained current-session controls, a Jobs application draft recovery unit contract covering safe draft storage/sync copy, raw quota/provider exclusion, preserved Review Application draft state, and Submit Application availability, a Post Job workflow contract covering company context creation/attach payloads, template save/apply/delete review, draft-history restore, duplicate review, and reviewed draft save payloads, a Post Job edit-draft recovery unit contract covering safe edit-draft context failed-load copy, raw error-message exclusion, and retry through the existing recruiter jobs load workflow, a Post Job browser storage recovery unit contract covering safe template save/delete and draft-history storage copy, raw quota/provider exclusion, and preserved form/review state, a Profile workflow contract covering AI profile draft save/discard, reviewed profile field saves, suggestion application, skill/experience/education row mutations, tab switching, and avatar upload/remove payloads, an avatar crop export fallback from canvas `toBlob` to data URL, a Resume workflow contract covering import text review, selected field apply/save, imported skill/experience/education saves, PDF/HTML downloads, provider PDF upload, export/artifact sync payloads, artifact copy/delete review, and AI resume draft apply/discard, a Settings workflow contract covering profile settings saves, keyboard notification preference changes, digest and quiet-hours saves, Billing summary/handoff, password reset review, account deactivation review, notification save failure retention/retry, and workflow analytics, a Billing workflow contract covering plan catalog/current-plan rendering, transaction history, plan review cancel/checkout handoff, billing portal handoff, provider checkout failure retention/retry, popup-blocked checkout warning, provider-unavailable retry recovery, demo-mode copy, and workflow analytics, a Billing action failure unit contract covering safe plan checkout and billing portal failure copy, raw provider-error exclusion, and retry through existing review modals, an AI Assistant workflow contract covering long-running generation state, provider failure/retry, save/dismiss review sync, audit/analytics payloads, clear-chat review/cancel/delete sync, and Profile/Resume/Jobs/Learning handoffs, a Career Path workflow contract covering generated guidance, review boundaries, malformed-data retry, provider-unavailable state, Learning handoff, and AI Assistant handoff, a Career Path provider recovery unit contract covering safe provider-unavailable copy, raw AI/provider-error exclusion, and retry through the existing career-path generation workflow, an Admin workflow contract covering source-labeled operational sections, scheduler verification state, audit pagination/retry, service investigation analytics hygiene, and scheduled automation status refresh, a Learning workflow contract covering AI handoff review, consumed route-state draft cleanup after search application, catalog search/pagination, enrollment, failed enrollment recovery, lesson completion, failed progress-persistence recovery, keyboard lesson selection/completion, and progress filtering, a Challenges workflow contract covering category filtering, workspace open, local sample-check result handling, unsupported-language and hidden-sample safeguards, reviewed reset, submission payloads, failed-submission recovery, latest result rendering, and retry-history refresh, a Challenges catalog failure unit contract covering safe failed-load copy, raw error-message exclusion, and retry through the existing fetch workflow, a Candidates workflow contract covering application pagination/search/focus, keyboard pagination/search/details/queue navigation, note deletion, scorecard fallback/retry, bulk status reviews, and failed status handling, a Messaging workflow contract that includes attachment-link keyboard focus order, uploaded-file attachments, failed-send retry, visible mark-read, and older-history loading, a Messaging action failure unit contract covering safe send/upload/mark-read failure copy, raw provider-error exclusion, failed-message retry, and unread-state preservation, a Networking workflow contract covering suggestion preview, hide/restore, connect, accept/decline, reminder set/clear, withdraw, accepted-profile preview, keyboard preview activation, and full-profile popup route targets, plus a Networking load failure unit contract covering safe failed-load copy, raw error-message exclusion, and retry through the existing suggestion fetch workflow; implementation scope now includes shell/primitives, safe fatal recovery, public/auth/recovery routes, role dashboards, major workspaces, legacy exported helper normalization, extension token hover cleanup, and documentation, with route/dashboard removal deferred until validated |
| Frontend public landing semantic contract | `apps/frontend/src/pages/LandingPage.tsx` and `apps/frontend/tests/auth.spec.ts` are present | Public landing navigation, role-entry CTAs, career preview rows, workflow control principles, platform pillars, feature-ownership decisions, and public stats expose named semantic structure; focused Chromium coverage verifies role-entry links, named public sections/lists, stats visibility, and 390px no-overflow rendering without changing typed Supabase public stat queries, fallback stats, route links, section anchors, auth handoff behavior, or public route behavior |
| Frontend Command search semantic contract | `apps/frontend/src/components/layout/CommandSearch.tsx`, `apps/frontend/src/components/layout/CommandSearch.test.tsx`, `apps/frontend/tests/command-search-workflow.spec.ts`, and `apps/frontend/tests/keyboard-navigation.spec.ts` are present | CommandSearch exposes a named Command search landmark, described combobox/listbox relationship, route label plus route-registry description option labels, live result status, named no-result status, and decorative search/result/arrow icons without changing route metadata, role filtering, result ranking, result count, shortcut behavior, keyboard navigation, route destinations, shell state, page behavior, or feature ownership |
| Frontend Shell navigation semantic contract | `apps/frontend/src/components/layout/Sidebar.tsx`, `apps/frontend/src/components/layout/Header.tsx`, `apps/frontend/src/components/layout/MobileMenu.tsx`, and `apps/frontend/tests/keyboard-navigation.spec.ts` are present | Collapsed desktop sidebar route/footer controls keep explicit accessible names, the Header mobile menu trigger exposes expanded/controlled state, the mobile slide-over exposes a distinct Expanded mobile navigation landmark with active-route state, and the legacy MobileMenu exposes named shortcut/menu navigation without changing route registry entries, mobile priority, role filtering, destinations, active matching, theme/sign-out behavior, or feature ownership |
| Frontend Shared toast accessibility contract | `apps/frontend/src/components/shared/Toast.tsx` and `apps/frontend/src/components/shared/Toast.test.tsx` are present | Shared transient toasts expose a named Toast notifications region, toast-type-specific status/alert labels, assertive atomic error alerts, notification-specific dismiss controls, and guarded auto-dismiss timing without changing toast APIs, durations, stack placement, page handlers, route behavior, or feature ownership |
| Frontend Legacy toast compatibility contract | `apps/frontend/src/context/ToastContext.tsx`, `apps/frontend/src/context/ToastContext.test.tsx`, and `apps/frontend/src/context/NotificationContext.tsx` are present | The older realtime notification toast provider preserves `showToast(type, message)`, click-to-dismiss, and 5-second auto-dismiss behavior while adding a named Realtime toast notifications region, status/alert labels, assertive atomic error alerts, and notification-specific keyboard dismiss controls without changing socket handlers, unread counts, notification payloads, route behavior, or feature ownership |
| Frontend Shared modal scroll containment contract | `apps/frontend/src/components/shared/AuraModal.tsx`, `apps/frontend/src/components/shared/AuraModal.test.tsx`, and `apps/frontend/tests/keyboard-navigation.spec.ts` are present | Shared review/confirmation modals preserve dialog naming, focus trap, Escape close, and opener focus restoration while adding reference-counted background page scroll lock/restoration without changing route-owned modal content, actions, service calls, route behavior, or analytics |
| Frontend Shared button presentation contract | `apps/frontend/src/components/shared/AuraButton.tsx` and `apps/frontend/src/components/shared/AuraButton.test.tsx` are present | Shared command buttons keep loading actions disabled and busy-labeled, preserve visible labels during loading, allow long text labels to wrap within parent surfaces, expose variant/size metadata for audits, and keep icon buttons dimensionally stable without changing route-owned click handlers, submit behavior, service calls, route behavior, or analytics |
| Frontend Shared page-header semantics contract | `apps/frontend/src/components/shared/PageHeader.tsx` and `apps/frontend/src/components/shared/PageHeader.test.tsx` are present | Shared page headers bind the H1 title and optional description to the header, preserve title-adjacent badges, and expose title-specific action groups without changing route-owned page titles, action controls, route behavior, service calls, or analytics |
| Frontend Shared card containment contract | `apps/frontend/src/components/shared/GlassCard.tsx` and `apps/frontend/src/components/shared/GlassCard.test.tsx` are present | Shared cards preserve caller-owned roles and labels while adding containment metadata, max-width/min-width constraints, wrapping-safe card titles/descriptions, min-width-safe content areas, and wrapping footers without changing route-owned card content, actions, route behavior, service calls, or analytics |
| Frontend Shared image presentation contract | `apps/frontend/src/components/shared/AuraImage.tsx` and `apps/frontend/src/components/shared/AuraImage.test.tsx` are present | Shared images preserve caller-owned alt text and load/error handlers while adding lazy/async defaults, named failed-image fallbacks, hidden decorative failed-image fallbacks, and source-change recovery without changing route-owned image choices, service calls, route behavior, or analytics |
| Frontend Shared tabs semantics contract | `apps/frontend/src/components/shared/Tabs.tsx` and `apps/frontend/src/components/shared/Tabs.test.tsx` are present | Shared tabs expose named horizontal tablists, stable tab/panel relationships when prefixes are supplied, decorative icon treatment, click selection, and Arrow/Home/End roving focus alignment without changing route-owned tab labels, tab state, route behavior, service calls, or analytics |
| Frontend legacy helper presentation contract | `apps/frontend/src/components/molecules/LegacyHelpers.test.tsx`, `StatCard.tsx`, `PostCard.tsx`, `SyncStatusBar.tsx`, and `shared/AuraStatusBar.tsx` are present | Exported legacy post/stat/status helpers expose named article, metric group, and status semantics with connected stat descriptions and decorative initials/icons hidden without changing helper props, visible copy, buttons, route behavior, service calls, or analytics |
| Frontend typography and page-template helper contract | `apps/frontend/src/components/atoms/Typography.tsx`, `apps/frontend/src/components/atoms/Typography.test.tsx`, `apps/frontend/src/components/templates/PageTemplate.tsx`, and `apps/frontend/src/components/templates/PageTemplate.test.tsx` are present | Exported typography helpers preserve native text semantics, caller attributes, wrapping-safe defaults, and caller class overrides; `PageTemplate` preserves optional shared headers and caller actions while adding named main content landmarks without changing route-owned controls, service calls, or analytics |
| Frontend responsive layout shell contract | `apps/frontend/src/components/shared/ResponsiveLayout.tsx` and `apps/frontend/src/components/shared/ResponsiveLayout.test.tsx` are present | Authenticated layout preserves userless passthrough, sidebar/header ownership, resize-driven sidebar offsets, and logout routing while exposing a named Application content landmark and shell slot metadata without changing route-owned workflows, service calls, or analytics |
| Frontend legacy navbar contract | `apps/frontend/src/components/shared/AuraNavbar.tsx` and `apps/frontend/src/components/shared/AuraNavbar.test.tsx` are present | Exported legacy navbar preserves public auth links, authenticated route destinations, profile/search/notification controls, mobile-menu close behavior, and logout routing while adding named navigation landmarks, active-route current state, controlled mobile-menu relationships, and decorative icon treatment without changing feature ownership |
| Frontend shared theme provider contract | `apps/frontend/src/components/shared/AuraThemeProvider.tsx` and `apps/frontend/src/components/shared/AuraThemeProvider.test.tsx` are present | Theme initialization preserves the `aura-theme` storage key, stored-theme priority, system preference fallback, root `light`/`dark` class synchronization, and current-session toggle behavior when browser storage is unavailable without changing shell theme controls, route behavior, service calls, or analytics |
| Frontend legacy mobile menu contract | `apps/frontend/src/components/layout/MobileMenu.tsx` and `apps/frontend/src/components/layout/MobileMenu.test.tsx` are present | Exported legacy mobile menu preserves link destinations, active-route state, controlled open/close callbacks, theme toggle, and sign-out behavior while exposing named shortcut and expanded navigation landmarks, limited shortcut-route placement, expanded-menu description relationships, and decorative icon treatment without changing feature ownership |
| Frontend active sidebar shell contract | `apps/frontend/src/components/layout/Sidebar.tsx` and `apps/frontend/src/components/layout/Sidebar.test.tsx` are present | Active sidebar preserves route-registry filtering, link destinations, active-route state, mobile close, theme toggle, sign-out, and collapse callbacks while adding explicit collapsed brand/home naming, decorative shell icon/divider treatment, hidden mobile overlay semantics, and focused unit coverage without changing feature ownership |
| Frontend active header shell contract | `apps/frontend/src/components/layout/Header.tsx` and `apps/frontend/src/components/layout/Header.test.tsx` are present | Active header preserves command search, notification loading/read/retry behavior, menu toggle callbacks, route navigation, service calls, and feature ownership while adding explicit shell button types, stable controlled relationships, and decorative treatment for shell icons, unread dots, and avatar initials |
| Frontend Shared input semantics contract | `apps/frontend/src/components/shared/AuraInput.tsx` and `apps/frontend/src/components/shared/AuraInput.test.tsx` are present | Shared text/email/password inputs bind visible labels, connect helper/error descriptions, mark error states with invalid semantics, preserve caller-provided descriptions, and hide decorative leading icons without changing route-owned validation copy, form submission behavior, service calls, route behavior, or analytics |
| Frontend Shared skeleton accessibility contract | `apps/frontend/src/components/shared/Skeleton.tsx` and `apps/frontend/src/components/shared/Skeleton.test.tsx` are present | Shared loading placeholders are decorative by default, preserve caller-provided status/name semantics when a loading placeholder must be announced, and use reduced-motion-aware pulse styling without changing route loading state ownership, layout dimensions, service calls, route behavior, or analytics |
| Frontend Shared badge semantics contract | `apps/frontend/src/components/shared/Badge.tsx` and `apps/frontend/src/components/shared/Badge.test.tsx` are present | Shared compact metadata badges preserve visible labels, support optional accessible descriptions for abbreviated labels, expose variant metadata for audits, constrain inline sizing, preserve caller-owned roles/data attributes, and avoid implicit live-region behavior without changing route-owned badge copy, route behavior, service calls, or analytics |
| Frontend Shared empty-state semantics contract | `apps/frontend/src/components/shared/EmptyState.tsx` and `apps/frontend/src/components/shared/EmptyState.test.tsx` are present | Shared no-data states expose named sections, connect descriptions, hide decorative icons, and preserve caller-owned recovery action callbacks without changing route-owned empty-state copy, action placement, service calls, route behavior, or analytics |
| Frontend Shared toggle semantics contract | `apps/frontend/src/components/shared/Toggle.tsx` and `apps/frontend/src/components/shared/Toggle.test.tsx` are present | Shared binary preference switches bind visible labels and descriptions to the switch, preserve fallback accessible names for unlabeled switches, and keep checked-state and disabled behavior stable without changing route-owned preference actions, service calls, route behavior, or analytics |
| Frontend Notification shell recovery contract | `apps/frontend/src/components/layout/Header.tsx` and `apps/frontend/src/components/layout/Header.test.tsx` are present | Header notification load failures and mark-all read persistence failures use safe popover copy, hide raw provider errors and token-like values, roll back unread state after failed read persistence, and retry through the existing Retry notifications and Mark read workflows without changing notification service calls, pagination, row navigation, scheduled reminder treatment, fallback labels, command search behavior, route behavior, or keyboard/focus behavior |
| Frontend Auth entry recovery contract | `apps/frontend/src/pages/auth/LoginPage.tsx`, `apps/frontend/src/pages/auth/RegisterPage.tsx`, `apps/frontend/src/pages/auth/authErrorCopy.ts`, and `apps/frontend/src/pages/auth/AuthEntry.test.tsx` are present | Login/Register auth provider failures use safe public copy, hide raw provider errors and token-like values, preserve invalid-credential and weak-password guidance, and keep registration role-intent context without changing Supabase auth calls, onboarding analytics, routes, or inactive provider/reset controls |
| Frontend Auth entry semantic contract | `apps/frontend/src/pages/auth/components/AuthShell.tsx`, `apps/frontend/src/pages/auth/LoginPage.tsx`, `apps/frontend/src/pages/auth/RegisterPage.tsx`, `apps/frontend/src/pages/auth/AuthEntry.test.tsx`, and `apps/frontend/tests/auth-entry-workflow.spec.ts` are present | Login/Register expose H1-named public auth main landmarks, named authentication panels, alternate-entry navigation, named Email sign-in and Account registration forms, Account Type group semantics, account-type control relationships, and a named registration next-step status without changing Supabase auth calls, registration role mapping, onboarding analytics, route queries, post-registration routes, auth redirects, inactive provider controls, or public reset-password visibility |
| Frontend Dashboard recovery contract | `apps/frontend/src/pages/dashboard/DashboardPage.tsx` and `apps/frontend/src/pages/dashboard/DashboardPage.test.tsx` are present | Dashboard partial and failed refresh issue rows use safe section labels, hide raw provider errors, retry through the existing dashboard refresh workflow, and expose metrics, onboarding tasks, quick actions, recent opportunities/applications, and active challenge summaries as named lists without changing service calls, analytics events, routes, role behavior, or destination actions |
| Frontend Jobs recovery contract | `apps/frontend/src/pages/jobs/JobsPage.tsx` and `apps/frontend/src/pages/jobs/JobsPage.test.tsx` are present | Jobs Explore catalog load failures, Applied-tab application history load failures, My Posts recruiter postings load failures, application-submit failures, and recruiter publish failures use safe retry copy, hide raw provider errors, and retry through the existing catalog refetch, application history load, recruiter job load, Review Application, or publish checklist workflows without changing query params, filters, saved searches, hidden Explore preferences, application drafts, submissions, details, recruiter postings, publish checklist rules, route behavior, or analytics behavior |
| Frontend Jobs preference recovery contract | `apps/frontend/src/pages/jobs/JobsPage.tsx` and `apps/frontend/src/pages/jobs/JobsPage.test.tsx` are present | Jobs saved-search and hidden Explore account-sync failures use safe local-fallback copy, hide raw provider errors, keep existing saved/hidden controls available, explain all-hidden Explore results when visibility preferences filter every current job, and preserve search/filter behavior, payloads, local storage keys, account sync service calls, restore behavior, route behavior, toast behavior, and analytics behavior |
| Frontend Jobs browser storage recovery contract | `apps/frontend/src/pages/jobs/JobsPage.tsx` and `apps/frontend/src/pages/jobs/JobsPage.test.tsx` are present | Jobs saved-search and hidden Explore browser-storage failures use safe local-storage copy, hide raw quota/provider errors, keep current-session saved/hidden controls available, retain hidden-job restore controls, and preserve payloads, local storage keys, account sync service calls, restore behavior, route behavior, toast behavior, and analytics behavior |
| Frontend Jobs application draft recovery contract | `apps/frontend/src/pages/jobs/JobsPage.tsx` and `apps/frontend/src/pages/jobs/JobsPage.test.tsx` are present | Jobs application draft browser-storage, draft-history browser-storage, and account-sync failures use safe modal-local copy, hide raw quota/provider errors, keep editable draft fields and Submit Application available, and preserve draft payloads, draft-history payloads, local storage keys, account sync service calls, application submission behavior, route behavior, toast behavior, and analytics behavior |
| Frontend Jobs result accessibility contract | `apps/frontend/src/pages/jobs/JobsPage.tsx` and `apps/frontend/src/pages/jobs/JobsPage.test.tsx` are present | Jobs Explore results, Applied applications, My Posts recruiter postings, and saved searches expose named list/listitem semantics without changing tab behavior, filters, saved-search behavior, hidden Explore preferences, application drafts, application submission, application details, recruiter postings, publish review, route behavior, toast behavior, or analytics behavior |
| Frontend Jobs application timeline accessibility contract | `apps/frontend/src/pages/jobs/JobsPage.tsx` and `apps/frontend/src/pages/jobs/JobsPage.test.tsx` are present | Applied application Details status timelines expose named region/list/listitem semantics for recorded status events, rejected fallback state, and inferred status steps without changing application loading, status-event loading, detail modal behavior, submitted details, draft behavior, application submission, saved searches, hidden Explore preferences, recruiter postings, route behavior, toast behavior, or analytics behavior |
| Frontend Post Job company context recovery contract | `apps/frontend/src/pages/jobs/PostJobPage.tsx` and `apps/frontend/src/pages/jobs/PostJobPage.test.tsx` are present | Post Job company context lookup failures use safe retry copy, hide raw provider errors and token-like values, keep editable draft and company form state available, and retry through the existing `companyService.getCompanyByUser(user.id)` workflow without changing route parameters, template or draft-history sync, company setup analytics, company create/update payloads, duplicate checks, publish behavior, navigation, or service ownership |
| Frontend Post Job action recovery contract | `apps/frontend/src/pages/jobs/PostJobPage.tsx` and `apps/frontend/src/pages/jobs/PostJobPage.test.tsx` are present | Post Job company create/update and draft save/update failures use safe action copy, hide raw provider errors and token-like values, preserve company form or draft review state, and retry through the existing Create & Attach Company, Save Company Profile, Save Draft, and Save Changes workflows without changing route parameters, template or draft-history sync, company setup analytics, company/job payloads, duplicate checks, publish behavior, navigation, or service ownership |
| Frontend Post Job browser storage recovery contract | `apps/frontend/src/pages/jobs/PostJobPage.tsx` and `apps/frontend/src/pages/jobs/PostJobPage.test.tsx` are present | Post Job template save/delete and draft-history browser-storage failures use accurate safe copy, hide raw quota/provider errors, keep current-session form or review state visible, and preserve template payloads, draft-history payloads, local storage keys, account sync service calls, route parameters, company setup analytics, duplicate checks, draft save/update behavior, navigation, and service ownership |
| Frontend Post Job section accessibility contract | `apps/frontend/src/pages/jobs/PostJobPage.tsx` and `apps/frontend/src/pages/jobs/PostJobPage.test.tsx` are present | Post Job exposes named workspace, form, template controls, draft history, company context, company profile details, editable draft fields, review metadata, requirement preview, duplicate/change lists, and action-group semantics without changing route parameters, template or draft-history sync, company setup analytics, company/create/update payloads, duplicate checks, draft save/update behavior, navigation, or service ownership |
| Frontend Profile recovery contract | `apps/frontend/src/pages/profile/ProfilePage.tsx` and `apps/frontend/src/pages/profile/ProfilePage.test.tsx` are present | Profile failed-load state uses safe retry copy, hides raw provider errors, and retries through the existing profile load workflow without changing route parameters, profile service ownership, edit modals, AI draft review, local suggestions, avatar upload/crop/remove, row mutations, tabs, toast behavior, or analytics behavior |
| Frontend Profile action recovery contract | `apps/frontend/src/pages/profile/ProfilePage.tsx` and `apps/frontend/src/pages/profile/ProfilePage.test.tsx` are present | Profile basic-save, completion-row save, row-delete, avatar-upload, and avatar-removal failures use safe inline copy, hide raw provider errors and token-like values, and retry through the existing Save Changes, Save, Remove, Upload Photo, and Remove Photo workflows without changing profile payloads, AI draft review, local suggestions, crop controls, row mutation services, tab behavior, toast behavior, or analytics behavior |
| Frontend Profile section accessibility contract | `apps/frontend/src/pages/profile/ProfilePage.tsx`, `apps/frontend/src/components/shared/Tabs.tsx`, and `apps/frontend/src/pages/profile/ProfilePage.test.tsx` are present | Profile summary, skill chips, summary metrics, completion checklist, local suggestions, tabpanels, experience rows, education rows, achievement cards, and completion progress expose named semantic structure without changing route parameters, profile load/update calls, edit modals, AI draft review, local suggestions, avatar upload/crop/remove, row mutation services, tab behavior, toast behavior, or analytics behavior |
| Frontend Resume recovery contract | `apps/frontend/src/pages/profile/ResumeBuilder.tsx` and `apps/frontend/src/pages/profile/ResumeBuilder.test.tsx` are present | Resume Builder profile-data load failures use safe retry copy, hide raw provider errors, and retry through the existing resume profile load workflow without changing editor fields, profile saves, import/AI draft review, profile-row saves, export/download/upload/copy/delete, artifact sync fallback, route behavior, or analytics behavior |
| Frontend Resume action recovery contract | `apps/frontend/src/pages/profile/ResumeBuilder.tsx` and `apps/frontend/src/pages/profile/ResumeBuilder.test.tsx` are present | Resume Builder save, provider upload, uploaded-PDF delete, detected-skill save, and detected-profile-row save failures use safe inline copy, hide raw provider errors and token-like values, and retry through the existing Save Changes, Upload PDF, Delete PDF, Save Skills, and Save Rows workflows without changing profile payloads, import parsing, AI draft review, export/download/upload/copy/delete commands, artifact sync fallback, route behavior, toast behavior, or analytics behavior |
| Frontend Resume editor accessibility contract | `apps/frontend/src/pages/profile/ResumeBuilder.tsx`, `apps/frontend/src/components/shared/Tabs.tsx`, and `apps/frontend/src/pages/profile/ResumeBuilder.test.tsx` are present | Resume actions, editor fields, import review, export activity, uploaded/deleted artifacts, export history, and preview sections expose named toolbar, region, list/listitem, and tabpanel semantics without changing profile load/update calls, import parsing, AI draft review, selected-field application, skill/row saves, export/download/upload/copy/delete commands, artifact sync fallback, route behavior, toast behavior, or analytics behavior |
| Frontend Settings recovery contract | `apps/frontend/src/pages/settings/SettingsPage.tsx` and `apps/frontend/src/pages/settings/SettingsPage.test.tsx` are present | Settings notification/billing data-load failures use safe retry copy, hide raw provider errors, and retry through the existing settings data load workflow without changing profile saves, notification preference edits/saves, security reviews, account deactivation, billing handoff, route behavior, toast behavior, or analytics behavior |
| Frontend Settings action recovery contract | `apps/frontend/src/pages/settings/SettingsPage.tsx`, `apps/frontend/src/pages/settings/components/ProfileSettings.tsx`, `apps/frontend/src/pages/settings/components/SecuritySettings.tsx`, and `apps/frontend/src/pages/settings/SettingsPage.test.tsx` are present | Settings profile-save, password-reset, and account-deactivation failures use safe inline copy, hide raw provider errors and token-like values, and retry through the existing Save Changes, Send Reset Email, and Confirm Deactivation workflows without changing profile payloads, reset/deactivation confirmation, billing handoff, notification saves, route behavior, toast behavior, or analytics behavior |
| Frontend Settings accessibility contract | `apps/frontend/src/pages/settings/SettingsPage.tsx`, Settings subcomponents, `apps/frontend/src/pages/settings/SettingsPage.test.tsx`, and `apps/frontend/tests/settings-workflow.spec.ts` are present | Settings section navigation exposes a named tablist with controlled tabpanels and keyboard movement, notification groups/security actions/billing metrics expose named list semantics, and the existing Settings browser workflow still verifies profile saves, notification saves, billing handoff, password reset review, and account deactivation review without changing service calls, route behavior, toast behavior, or analytics behavior |
| Frontend Billing recovery contract | `apps/frontend/src/pages/billing/BillingPage.tsx` and `apps/frontend/src/pages/billing/BillingPage.test.tsx` are present | Billing provider-unavailable load failures, plan checkout failures, and billing portal failures use safe retry copy, hide raw provider errors and token-like values, expose accessible plan-comparison semantics, and retry through the existing billing data load, Review Plan, and Update Payment Method workflows without changing plan, history, subscription, checkout, portal, demo-mode, route, toast, or analytics behavior |
| Frontend Billing payment/history accessibility contract | `apps/frontend/src/pages/billing/BillingPage.tsx` and `apps/frontend/src/pages/billing/BillingPage.test.tsx` are present | Billing payment method and transaction history expose named region/group/list semantics with transaction labels without changing payment-service plan/history/subscription loads, checkout/session commands, billing portal commands, review modals, retry behavior, toast behavior, explicit demo mode, route behavior, or analytics behavior |
| Frontend AI Assistant recovery contract | `apps/frontend/src/pages/ai/AIAssistant.tsx` and `apps/frontend/src/pages/ai/AIAssistant.test.tsx` are present | AI Assistant chat provider failures use safe draft copy, hide raw provider errors, and retry through the existing chat response workflow without changing chat persistence, review queue, save/dismiss sync, analytics/audit payloads, clear-chat review, route behavior, or destination workflow handoffs |
| Frontend AI Assistant review accessibility contract | `apps/frontend/src/pages/ai/AIAssistant.tsx` and `apps/frontend/src/pages/ai/AIAssistant.test.tsx` are present | AI review queue draft cards, the chat history, and empty-state prompt suggestions expose named list/log/article semantics without changing prompt selection, chat generation, chat persistence, review status, analytics/audit payloads, clear-chat review, route behavior, or destination workflow handoffs |
| Frontend AI Assistant presentation contract | `apps/frontend/src/pages/ai/AIAssistant.tsx` and `apps/frontend/src/pages/ai/AIAssistant.test.tsx` are present | AI Assistant badge, review-queue, recommendation-card, conversation-avatar, draft-prompt, and composer icons are decorative while controls remain named by visible text or explicit labels, without changing session load/save/delete, prompt selection, chat generation/retry, review save/dismiss, clear-chat review, workflow handoffs, service calls, route behavior, or analytics |
| Frontend Career Path guidance accessibility contract | `apps/frontend/src/pages/ai/AICareerPath.tsx`, `apps/frontend/src/pages/ai/AICareerPath.test.tsx`, and `apps/frontend/tests/career-path-workflow.spec.ts` are present | Career Path generated guidance exposes a named workspace, generated-path region, required-skill list/listitem semantics, milestone list/listitem labels with completed/pending state, and review-boundary region/list semantics without changing AI service generation ownership, response normalization, load/retry behavior, fallback milestones, Learning navigation, AI Assistant navigation, provider-unavailable state, route behavior, or review-only source-record guarantees |
| Frontend Career Path presentation contract | `apps/frontend/src/pages/ai/AICareerPath.tsx` and `apps/frontend/src/pages/ai/AICareerPath.test.tsx` are present | Career Path unavailable-state, retry, AI handoff, generated-path, milestone-state, Learning handoff, and review-boundary icons are decorative while generated guidance meaning stays in headings, region/list labels, listitem labels, and visible button text, without changing AI generation, retry, Learning navigation, AI Assistant navigation, route behavior, service calls, or review-only guarantees |
| Frontend Messaging recovery contract | `apps/frontend/src/pages/messaging/MessagingPage.tsx` and `apps/frontend/src/pages/messaging/MessagingPage.test.tsx` are present | Messaging conversation-list and selected message-history load failures plus send, attachment upload, and visible mark-read action failures use safe retry/status copy, hide raw provider errors and token-like values, keep failed messages or unread state available, expose descriptive conversation-row/current-state labels with presence status, hide decorative conversation-list search/retry icons, fallback initials, online dots, thread/composer icons, thread avatar initials, and realtime status dots, expose descriptive thread-action labels, and retry through the existing conversation/message-history load, message Retry failed message, Upload file, and mark-read workflows without changing conversation fetches, message fetches, realtime subscription, pagination, send payloads, mark-read payloads, attachment behavior, route, or analytics behavior |
| Frontend Networking action recovery contract | `apps/frontend/src/pages/networking/NetworkingPage.tsx` and `apps/frontend/src/pages/networking/NetworkingPage.test.tsx` are present | Networking Connect, incoming Accept, incoming Decline, and sent-request Withdraw failures use safe inline copy, hide raw provider errors and token-like values, expose accessible card-list semantics and person-specific repeated action labels, hide decorative search/card/action/status/metadata/preview icons plus avatar initials, and retry through the existing Connect, Accept, Decline, and Withdraw workflows without changing suggestion fetches, connection-state loads, notes, hidden suggestions, reminders, profile preview, route behavior, toast behavior, or analytics behavior |
| Frontend Admin recovery contract | `apps/frontend/src/pages/admin/AdminDashboard.tsx` and `apps/frontend/src/pages/admin/AdminDashboard.test.tsx` are present | Admin Console top-level load failures use safe retry copy, hide raw service errors, and retry through the existing refresh workflow without changing stats, service-health, scheduler, analytics, audit, role, route, or analytics behavior |
| Frontend Admin operational accessibility contract | `apps/frontend/src/pages/admin/AdminDashboard.tsx` and `apps/frontend/src/pages/admin/AdminDashboard.test.tsx` are present | Admin summary metrics, product analytics, scheduled automation, service health, and audit log surfaces expose named regions, lists, tables, and row labels without changing admin stats calls, audit pagination, product analytics loading, scheduler status loading, refresh handlers, observability links, source labels, role gates, route behavior, or admin analytics events |
| Frontend Admin presentation contract | `apps/frontend/src/pages/admin/AdminDashboard.tsx` and `apps/frontend/src/pages/admin/AdminDashboard.test.tsx` are present | Admin refresh, failed-load, degraded-state, metric, analytics, scheduler, service-health, observability-link, and audit-log icons are decorative while operational meaning stays in headings, regions, table/list labels, row labels, visible status text, and explicit button/link text, without changing refresh/retry workflows, scheduler status, observability links, audit pagination, route behavior, service calls, role gates, or analytics |
| Frontend Learning recovery contract | `apps/frontend/src/pages/lms/LMSPage.tsx` and `apps/frontend/src/pages/lms/LMSPage.test.tsx` are present | Learning course-catalog and enrolled-progress load failures plus enrollment and lesson-completion persistence failures use safe retry/status copy, hide raw provider errors and token-like values, preserve already loaded course/progress state, retry through existing catalog fetch, enrollment progress load, Enroll Now, and Mark Complete workflows, and expose Continue Learning/catalog-card/course-detail progress tracks as contextual progressbars without changing Redux query state, cursor state, tab behavior, search behavior, AI suggestion review, enrollment payloads, lesson-completion payloads, progress warnings/calculation, route behavior, or analytics behavior |
| Frontend Challenges recovery contract | `apps/frontend/src/pages/challenges/ChallengesPage.tsx` and `apps/frontend/src/pages/challenges/ChallengesPage.test.tsx` are present | Challenges catalog load failures and submission persistence failures use safe retry/status copy, hide raw provider errors and token-like values, keep the challenge workspace and retry history available, retry through the existing fetch and Submit Solution workflows, and expose challenge catalog/workspace list and region semantics without changing Redux fetch behavior, category filtering, workspace state, language selection, starter-code reset review, local sample checks, hidden-sample safeguards, submit payloads, retry-history refresh, route behavior, toast behavior, or analytics behavior |
| Frontend Candidates recovery contract | `apps/frontend/src/pages/candidates/CandidatesPage.tsx` and `apps/frontend/src/pages/candidates/CandidatesPage.test.tsx` are present | Candidates application-list load failures use safe retry copy, hide raw provider errors, and retry through the existing candidate application page load workflow without changing candidate search, cursor paging, review focus, private notes, scorecards, interview-plan drafts, status updates, bulk review, route behavior, or analytics behavior |
| Frontend Candidates action recovery contract | `apps/frontend/src/pages/candidates/CandidatesPage.tsx` and `apps/frontend/src/pages/candidates/CandidatesPage.test.tsx` are present | Candidates single-status and all-failed bulk status failures use safe confirmation-modal copy, hide raw provider errors and token-like values, keep failed eligible applications available for retry, and retry through the existing status and bulk confirmation workflows without changing status payloads, eligibility rules, private notes, scorecards, interview-plan drafts, cursor state, route behavior, toast behavior, or analytics behavior |
| Frontend Candidates list accessibility contract | `apps/frontend/src/pages/candidates/CandidatesPage.tsx` and `apps/frontend/src/pages/candidates/CandidatesPage.test.tsx` are present | Candidate review metrics expose named list semantics with scorecard coverage, average rubric, evidence gap, and scorecard sync labels; candidate application rows expose named list semantics with candidate, job, status, and email context without changing search, cursor paging, review focus, private notes, scorecards, review queue, status actions, bulk review, route behavior, toast behavior, or analytics behavior |
| Frontend Candidates details accessibility contract | `apps/frontend/src/pages/candidates/CandidatesPage.tsx` and `apps/frontend/src/pages/candidates/CandidatesPage.test.tsx` are present | Candidate Details exposes named regions/lists for queue navigation, identity summary, application metadata, advisory signal, submitted materials, review guidance, interview-plan slots, scorecard dimensions, and recruiter notes without changing search, cursor paging, review focus, private notes, scorecards, review queue, status actions, bulk review, route behavior, toast behavior, or analytics behavior |
| Companion extension | `chrome-extension-project` exists with its own package lock and Manifest V3 code; `npm run test:messaging` validates content scan extraction and background/content message wiring, `npm run test:portal-fixtures` validates LinkedIn/Indeed/Glassdoor selector fixture parsing, `npm run test:options-ux` validates Resume Match missing/short/large text guidance, Interview Planner labels/live validation/stateful card toggles/reset review relationships, safe options storage failure warnings, shared storage removed-key fallback, accessible live status semantics, character counts, result-region semantics, and raw pasted-text/prep-topic diagnostics exclusion, `npm run test:popup-ux` validates safe popup scan status copy, safe popup storage failure warnings, limited scanned-draft review warnings, alert/status semantics, raw page-scan error exclusion from visible logs, and raw quota/runtime/storage-key exclusion from popup storage copy, `npm run test:storage-migrations` validates versioned local storage migration logic and install/update wiring, `npm run test:contract` validates MV3 permissions/hosts, local-only sync posture, bounded diagnostics, metadata allowlists, and raw resume/job diagnostics exclusion, and `npm run test:runtime-smoke` validates the built artifact, host-mapped portal fixture tabs, popup, options page, local storage, install-time storage schema marker, background messaging, Resume Match validation/report behavior, Interview Planner prep-card create/toggle persistence, Prep Clear All cancel/confirm, Settings reset cancel/confirm, popup storage load and quota-pressure save warnings, and options prep/settings storage load and quota-pressure save warnings in a live Chromium-compatible MV3 runtime with a Node 20-compatible CDP transport | Separate local extension companion, not cloud-synced to web app; source-level portal fixture parsing, options Resume Match UX, options Interview Planner accessibility, options storage failure visibility and removed-key fallback, popup scan recovery UX, popup storage failure visibility, built options runtime flow coverage including reviewed prep clear/reset, install-time schema marker creation, and popup/options storage-warning plus quota-pressure save-warning rendering, host-mapped runtime portal fixture execution, manifest icons, and Chromium runtime popup/options/storage/background smoke are verified and wired into the extension CI job, while GitHub-hosted browser availability, live public-portal drift, Google Chrome-specific unpacked registration, true storage-capacity exhaustion, Web Store packaging, screen-reader walkthroughs, all-browser options rendering, published prior-version update migration, and published behavior remain pending |
| Product data access | Frontend services use Supabase directly for many workflows and API Gateway/Spring fallbacks for others | Explicit hybrid, not a pure backend-owned API layer |
| Schema authority | `data-ownership-manifest.json`, `supabase-schema.sql`, `infra/db/migrations/0001_initial_baseline.sql`, `infra/db/generated/database.types.ts`, `infra/db/legacy-schema-disposition.json`, `apps/frontend/src/lib/supabaseClient.ts`, `scripts/validate-typed-supabase-boundary.mjs`, `infra/supabase_master.sql`, and ADR-003 are present | ADR-003 accepts migration-first Supabase/Postgres authority with generated TypeScript types and backend validation; the initial baseline migration, source-derived relationship-aware generated types, frontend `typedSupabase` boundary, generated RPC typing, legacy/duplicate table dispositions, trigger insert-column validation, and typed billing/challenge/application/settings service migrations are source-validated, while live Supabase migration execution, smaller domain-ordered migrations, live Supabase-generated relationship types, live RLS behavior, query-plan/index validation, and remaining service-by-service typed repository adoption remain pending |
| Messaging boundary | `services/messaging-service`, orphaned `services/chat-service`, frontend messaging Supabase access, API contract report, OpenAPI contract, and ADR-004 are present | ADR-004 accepts one messaging domain boundary; `messaging-service` remains active, `/api/v1/chat/*` remains non-active orphaned surface, and runtime WebSocket/STOMP behavior remains pending |
| Payment mode | `apps/frontend/src/services/paymentService.ts`, `BillingPage.tsx`, `services/payment-service`, API contract report, OpenAPI contract, billing SQL, and ADR-005 are present | ADR-005 accepts explicit demo billing mode; provider-backed checkout, signed webhooks, webhook-owned subscription/payment state, and provider runtime tests remain pending |
| Backend topology | `services/*-service` Spring modules plus `api-gateway`, shared modules, contracts, schemas, BOM, service-parent POMs, and ADR-002 are present | Service-module architecture remains active while ADR-002 accepts modular monolith first with extractable service boundaries as the rebuild target |
| Unified backend target | `apps/backend` contains only `src/main/resources/application.yml` | Single-backend rebuild is not implemented; ADR-002 accepts the target topology but not runtime completion |
| Service dependency model | Many service POMs use `talentsphere-bom` and `ts-contracts`, but many still depend on `ts-shared` | BOM/contracts migration is partial, not complete decoupling |
| Gateway/API security alignment | Generated API contract report currently shows 0 unmatched frontend API client calls, 0 controller routes without gateway prefixes, and 0 legacy `/api/*` security matcher paths; generated OpenAPI payload contract covers 123 active controller operations, 56 source-derived component schemas, parameters, request bodies, and response bodies; ADR-001 accepts Supabase Auth as primary identity provider; production secret validation is source-wired through `MandatoryEnvironmentPostProcessor`; shared exception handling now returns safe public codes with correlation IDs; file-service upload policy now checks MIME, content signatures, active content, and a malware scanner hook before local storage; Gateway sensitive routes have source-level Redis rate-limit wiring; service-role scheduler scripts now write sanitized commit-mode run audits; CI now has source-level dependency, secret, misconfiguration, and container scan gates; source-level observability catalogs cover 12 critical alert/dashboard flows with runbook links | First route/security drift pass is clean; `npm run validate:api-openapi-contract` enforces source-derived OpenAPI operation/schema consistency; `npm run validate:auth-contract` enforces the source-level Supabase/HMAC Gateway contract, exact public-route matching, normalized `ROLE_*` forwarding, sensitive-route rate-limit wiring, and default-disabled backend local credentials; `npm run validate:security-contract` enforces source-level production secret fail-fast behavior, Kubernetes runtime secret wiring, safe public error handling, file upload content/security policy, audited service-role scheduler run wiring, and CI security scan wiring; `npm run validate:observability-contract` enforces source-level alert/dashboard/runbook coverage; Maven execution, runtime Springdoc output, deployed dashboards/Alertmanager, provider-backed antivirus, runtime Redis rate-limit behavior, live scheduler execution, live CI scan execution, and live Supabase token verification remain pending |
| Backend test execution | No `mvn`, `mvnw`, or `gradlew` is available in the current workspace environment | Java test claims require an environment with a build runner |
| Frontend validation | `npm run lint`, `npm run test:unit`, and `npm run build` have been passing in the active implementation loop | Frontend can be validated locally |

## 3. Reconciled Historical Documents

| Document | Existing claim or purpose | Current interpretation |
|---|---|---|
| `SSOT.md` | Claims "Production Ready & Decoupled", "all phases complete", and "supersedes all previous documentation" | Historical reference. Some route/service information remains useful, but completion and authority claims are stale. |
| `docs/ARCHITECTURE_AUDIT.md` | Describes a distributed-monolith problem and proposes micro-frontends/polyrepo-style service independence | Historical risk assessment. It is directionally useful for coupling risks, but it is not the current implementation state. |
| `docs/SERVICE_MIGRATION.md` | Says a subset of services were updated and many remain pending | Historical migration tracker. Current POM evidence shows broader BOM/contracts usage, but `ts-shared` remains in many services, so migration is still incomplete. |
| `docs/ARCHITECTURE_MIGRATION.md` | Says contracts, BOM, module federation, shared split, and import migration are done | Historical progress notes. Treat import migration claims carefully because `ts-shared` dependency still exists in many service POMs. |
| `docs/ARCHITECTURE_PROPOSAL.md` | Proposes resilient event-mesh improvements such as outbox, DLQ, schema registry, caching, tracing | Future-state proposal. Use it as an improvement source, not as current-state evidence. |
| `docs/unified-rebuild-roadmap.md` | Targets one Spring Boot backend runtime and one frontend runtime | Strategic alternative roadmap. The single-backend target is not current repo state. |
| `docs/system/README.md`, `docs/frontend/README.md`, `docs/backend/README.md` | Extracted/generated-style system docs with complete architecture and endpoint claims | Historical generated references. Use current source, `docs/API_CONTRACT_MISMATCH_REPORT.md`, and `docs/FEATURES_AND_DASHBOARDS.md` for current behavior. |
| `docs/DATABASE_SHARDING.md`, `docs/OPERATIONAL_RUNBOOK.md` | Citus sharding and older environment runbook content | Proposal/draft material. Production Citus, backups, SLOs, placeholder contacts, and cluster operations are Not verified from the codebase; use `docs/runbooks/INCIDENT_RUNBOOKS.md` for current source-backed incident response. |
| `AGENT_OWNERSHIP.md`, `CLAUDE.md`, `GEMINI.md`, `ISSUES.md` | Historical agent coordination/context and issue-tracker notes | Historical/stale references with outdated environment, ownership, or completion assumptions. Use `../../PLAN.md` and `module-manifest.json` instead. |
| `README.md` | Presents Supabase-first setup and says `SSOT.md` is the master architecture document | Useful onboarding entry, but its documentation map now points readers to this status index first. |

## 4. Current Operating Assumptions

- Treat the active product as a React/Vite web app backed by Supabase for many product workflows, with Spring service modules and API Gateway available for backend-owned or fallback paths.
- Do not assume the backend is fully decoupled: `ts-shared` remains a service dependency in many POMs.
- Do not assume the one-backend rebuild is complete: `apps/backend` is not a complete backend application.
- Do not assume all API paths or payloads are correct from static docs alone: refresh `docs/API_CONTRACT_MISMATCH_REPORT.md` with `npm run report:api-contracts` and `docs/API_OPENAPI_CONTRACT.json` with `npm run report:api-openapi`.
- Treat user-controlled automation as the product standard: automation may draft, rank, prefill, alert, or summarize, but critical workflow changes require explicit user approval.
- Treat local-only and fallback-only state as a product risk when users reasonably expect cross-device continuity or auditability.

## 5. Open Architecture Decisions

| Decision | Why it matters | Recommended next step |
|---|---|---|
| Primary identity provider | Frontend Supabase Auth and backend auth-service local JWT login previously overlapped | ADR-001 accepted Supabase Auth as the primary login/session authority; backend local credentials are disabled by default behind `AUTH_LOCAL_CREDENTIALS_ENABLED`; run live token verification in an integration environment and finish identity bootstrap/retirement decisions |
| Product data ownership | Direct Supabase and Spring services both touch product domains, which complicates security, caching, auditing, and observability | Choose explicit hybrid ownership rules or move toward a backend-owned product API layer |
| Backend topology | Docs previously disagreed between service-module independence and a single-backend rebuild | ADR-002 accepts modular monolith first with extractable service boundaries; define the target package skeleton and migration sequence while active services remain current source evidence |
| Schema authority | Direct frontend table access, duplicate SQL sources, and unresolved live RLS/index statuses complicate safe migrations | ADR-003 accepts migration-first Supabase/Postgres authority; keep the baseline migration, relationship-aware generated DB types, `typedSupabase` boundary, typed Supabase boundary validator, trigger/function insert validation, and legacy disposition matrix synchronized, decompose `supabase-schema.sql` into smaller ordered migrations, migrate/retain/retire `infra/supabase_master.sql` legacy tables, migrate remaining direct services into typed repositories, and validate live RLS/indexes table by table |
| Messaging boundary | `messaging-service` and orphaned `chat-service` previously overlapped | ADR-004 accepts one messaging domain boundary; retire `chat-service` or merge useful realtime adapter code into the messaging domain after authZ/realtime validation |
| Payment mode | Billing UI, frontend Supabase billing functions, backend synthetic payment sessions, and Stripe scaffolding previously implied mixed readiness | ADR-005 accepts explicit demo billing mode; implement provider-backed checkout/webhooks only after signed webhook, idempotency, audit, and runtime provider validation exist |
| Shared dependency migration | `ts-shared` still couples many services | Create a service-by-service dependency removal plan with build verification |
| Pagination strategy | Jobs Explore, Candidates, Messaging conversation lists, Messaging active threads, Header notifications, LMS course catalog/progress tabs, and Admin audit logs now use cursor-backed older-item loading; remaining backend-owned APIs still need formal cursor contracts and total-count metadata where appropriate | Add formal cursor and total-count contracts to backend-owned chat/notification/LMS APIs and any new high-scale lists |
| Observability source | Admin UI still has fallback/mock-style service health in places | Define real service health, trace, log, and incident-link contracts |
| Documentation lifecycle | Docs must stay classified so stale, proposal, generated, and current references are not confused | `module-manifest.json`, top-level `Documentation status:` banners, and `npm run validate:docs-lifecycle` now enforce lifecycle classification; keep them updated with new docs |
| Incident runbook scope | Source-level incident commands and runtime environment runbooks can drift | `docs/runbooks/INCIDENT_RUNBOOKS.md` is current and validated by `npm run validate:runbooks`; runtime cluster/provider runbooks remain draft/unverified until environment evidence exists |
| Observability source | Alert and dashboard coverage can drift from critical flows and runbooks | `infra/observability/alerts/critical-alerts.json`, `infra/observability/dashboards/critical-flows-dashboard.json`, and `npm run validate:observability-contract` enforce source-level coverage; deployed dashboard and alert-manager behavior is Not verified from the codebase |

## 6. Validation Commands

Use this quick validation set after architecture or API-facing changes:

```bash
npm run report:api-contracts
npm run validate:module-manifest
npm run validate:infrastructure-manifest
npm run validate:docs-lifecycle
npm run validate:runbooks
npm run validate:observability-contract
npm run validate:ui-design-system
npm run test:ia
npm run test:a11y
npm run test:keyboard
npm run validate:backend-topology-adr
npm run validate:schema-authority-adr
npm run report:db-types
npm run validate:schema-migrations
npm run validate:typed-supabase-boundary
npm run validate:messaging-boundary-adr
npm run validate:payment-mode-adr
npm run validate:data-ownership
npm run validate:auth-contract
npm run validate:security-contract
npm run report:api-openapi
npm run validate:api-openapi-contract
npm run test:extension-messaging
npm run test:extension-portal-fixtures
npm run test:extension-storage-migrations
npm run test:extension-contract
npm run test:extension-runtime-smoke
npm run lint
npm run test:unit
npm run build
git diff --check
```

Backend validation requires Maven or a wrapper that is not present in the current workspace environment.

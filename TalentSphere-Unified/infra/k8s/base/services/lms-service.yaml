apiVersion: apps/v1
kind: Deployment
metadata:
  name: lms-service
  labels:
    app: lms-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lms-service
  template:
    metadata:
      labels:
        app: lms-service
    spec:
      containers:
      - name: lms-service
        image: talentsphere/lms-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8085
        envFrom:
        - configMapRef:
            name: talentsphere-config
        - secretRef:
            name: talentsphere-secrets
        resources:
          requests:
            memory: 256Mi
            cpu: 100m
          limits:
            memory: 512Mi
            cpu: 500m
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8085
          initialDelaySeconds: 15
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8085
          initialDelaySeconds: 15
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: lms-service
  labels:
    app: lms-service
spec:
  selector:
    app: lms-service
  ports:
  - port: 8085
    targetPort: 8085

apiVersion: apps/v1
kind: Deployment
metadata:
  name: networking-service
  labels:
    app: networking-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: networking-service
  template:
    metadata:
      labels:
        app: networking-service
    spec:
      containers:
      - name: networking-service
        image: talentsphere/networking-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8095
        envFrom:
        - configMapRef:
            name: talentsphere-config
        resources:
          requests:
            memory: 256Mi
            cpu: 100m
          limits:
            memory: 512Mi
            cpu: 500m
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8095
          initialDelaySeconds: 15
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8095
          initialDelaySeconds: 15
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: networking-service
  labels:
    app: networking-service
spec:
  selector:
    app: networking-service
  ports:
  - port: 8095
    targetPort: 8095

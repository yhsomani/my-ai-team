# Frontend API URL Standardization
This PR standardizes the environment variable used for the API base URL in the frontend.
Replaces instances of `VITE_API_URL` with `VITE_API_BASE_URL` for consistency with other parts of the application.
